[project]
name = "cloud-janitor"
version = "0.1.0"
description = "Multi-agent AWS infrastructure auditor and remediation system using MCP protocol"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.28.1",
]
